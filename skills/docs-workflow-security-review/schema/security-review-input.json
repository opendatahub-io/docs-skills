{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "security-review step input",
  "description": "CLI args contract for the security-review workflow step.",
  "type": "object",
  "required": [
    "ticket",
    "base_path"
  ],
  "properties": {
    "ticket": {
      "type": "string",
      "description": "Jira ticket ID (e.g. PROJ-123)."
    },
    "base_path": {
      "type": "string",
      "description": "Base path for the workflow workspace."
    }
  },
  "additionalProperties": false
}
