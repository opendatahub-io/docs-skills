The research step fetched several pages from the product documentation and found
that the mirror registry requires a pull secret before any image can be copied
into it, which means the credentials have to exist before anyone starts the
mirroring procedure at all, under every supported configuration.
